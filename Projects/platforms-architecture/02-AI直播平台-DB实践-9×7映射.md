---
title: AI 直播平台 DB 实践 × 9×7 矩阵映射
tags: [AI直播/数据库/9×7映射/checklist串联]
created: 2026-06-25
updated: 2026-06-25
status: 串联完成
parent_db_framework: 00-数据库开发全流程-极致深度框架-9×7矩阵.md
parent_checklist: 00-AI直播平台落地checklist.md
db_stack: PostgreSQL 16 (主) + Redis 7 (缓存) + ClickHouse (分析,可选)
---

# AI 直播平台 DB 实践 × 9×7 矩阵映射

> **目的**:把 `00-AI直播平台落地checklist.md` 中所有 DB 相关项,**映射到 9×7 数据库框架的对应列**,便于按列组织任务清单。
> **映射原则**:每项 checklist 任务,在 9×7 框架中归属某一列(可能跨列),按列汇总即可形成"AI 直播 DB 实战路线图"。

---

## 一、Phase 0 (MVP) DB 任务 × 9×7 列映射

| Checklist 任务(原行) | 9×7 列 | 二级子模块 | 关键实施点 |
|------------------|--------|----------|----------|
| L116 MySQL 8 + 主从(改为 PG) | **A 结构** | A2 主键约束 + A3 分区分表 | `BIGSERIAL` 主键 + 流复制 |
| L117 Redis 单实例 | **F 指标** | F1 QPS/TPS | QPS < 10K 用单实例足够 |
| L118 Redis Stream 消息队列 | **B 逻辑** | B2 事务锁 | Stream + Consumer Group 实现轻量 MQ |
| L119 OSS 对象存储 | **G 规则** | G4 容灾 | 直播录制 + 商品图片走 OSS |
| L120 MySQL FULLTEXT 搜索 | **C 配置** | C1 索引类型 | PG 用 `tsvector + GIN` 替代 |

### 🔧 MVP 阶段 9×7 实战清单(7 列)

#### A 列(结构)
- [ ] 用户/订单/商品/直播间 4 张核心表建表(A1-A4)
- [ ] `BIGSERIAL PRIMARY KEY` 主键策略统一
- [ ] `NUMERIC(20,4)` 财务字段精度锁死
- [ ] `TIMESTAMPTZ` 全时间字段带时区

#### B 列(逻辑)
- [ ] 所有写 SQL 走 `BEGIN; ... COMMIT;` 短事务
- [ ] 库存扣减用 `FOR UPDATE` + 短事务(< 100ms)
- [ ] 弹幕写入 Redis Stream 而非 PG
- [ ] 慢 SQL 走 `pg_stat_statements` 监控

#### C 列(配置)
- [ ] 核心查询建 B-Tree 复合索引
- [ ] 商品 JSONB 属性建 GIN 索引
- [ ] 时序大表(直播事件)建 BRIN 索引
- [ ] `random_page_cost = 1.1` (SSD 调优)

#### D 列(用例)
- [ ] pgTAP 覆盖 4 张核心表(插入/更新/删除/约束)
- [ ] pgbench 压测:32 并发 60s,记录基线 TPS
- [ ] 关键接口压测:登录/开播/下单/支付

#### E 列(校验)
- [ ] ER 图 Mermaid 落地:`USERS / ORDERS / LIVE_ROOMS / PRODUCTS`
- [ ] 业务主键与物理主键分离(`id BIGSERIAL` + `biz_id VARCHAR`)
- [ ] 状态机:`orders.status` 1=待支付 2=已支付 3=已发货 4=已完成 5=退款
- [ ] CHECK 约束:`balance >= 0`、`status IN (...)`

#### F 列(指标)
- [ ] pg_exporter + Prometheus + Grafana 三件套
- [ ] QPS / TPS / 慢查询 / 死锁 4 仪表盘
- [ ] 告警:P99 > 200ms / 连接数 > 80% / 死锁 > 0
- [ ] 每日巡检脚本:bloat / 索引命中率 / 复制延迟

#### G 列(规则)
- [ ] 3 角色:`openlive_app` / `openlive_readonly` / `openlive_audit`
- [ ] 最小权限:app 只能 SELECT/INSERT/UPDATE/DELETE,无 DDL
- [ ] 密码 90 天轮换 + `VALID UNTIL`
- [ ] pgaudit 开启,记录所有写操作

---

## 二、Phase 1 (规模化 1-10万 DAU) DB 任务 × 9×7 列映射

| Checklist 任务(原行) | 9×7 列 | 二级子模块 | 关键实施点 |
|------------------|--------|----------|----------|
| L127 分库分表 ShardingSphere/TiDB | **A 结构** + **B 逻辑** | A3 分区分表 + B4 批处理 | 按 `user_id % 16` 分 16 库 |
| L130 Redis Cluster | **F 指标** + **G 规则** | F1 + G4 | Cluster 3 主 3 从 |
| L131 自建 MinIO | **G 规则** | G4 容灾 | 多副本 + 跨机架 |
| L132 Elasticsearch | **C 配置** + **B 逻辑** | C1 索引类型 + B1 查询语句 | 笔记/评论/弹幕搜索 |
| L407 分布式事务 Seata/Saga | **B 逻辑** + **G 规则** | B2 事务锁 + G1 权限 | 订单支付链路 |

### 🔧 规模化阶段 9×7 实战清单

#### A 列
- [ ] 用户表按 `user_id % 16` 水平分 16 库 64 表
- [ ] 订单表按 `user_id` 分 + 按 `created_at` 分月归档
- [ ] 商品表按 `category_id` 范围分区
- [ ] 历史数据冷热分离:1 年前数据走 OSS + 列存

#### B 列
- [ ] 分布式事务:下单→扣库存→扣款 Saga 编排
- [ ] 跨库 JOIN 禁止,改走 `WHERE user_id IN (...)` 多次查询合并
- [ ] 全局唯一 ID:雪花算法(Snowflake)64bit
- [ ] `pg_cron` 定时清理过期数据 + 归档

#### C 列
- [ ] Elasticsearch 双索引:近实时 + 历史
- [ ] 异步写入 Kafka → ES(弹幕/评论不阻塞主库)
- [ ] 读路径:Redis 缓存(短)→ ES 全文(中)→ PG 实时(长)
- [ ] 预计算:MV 物化视图 + 定时刷新

#### D 列
- [ ] 混沌演练:节点宕机 / 网络分区 / 磁盘满
- [ ] 故障注入:`tc netem` 加 200ms 延迟 / `dd` 灌满磁盘
- [ ] 回归测试:每次 schema 变更跑 500 条 SQL 回归
- [ ] 性能基线:QPS / P99 / 错误率三指标每版本比对

#### E 列
- [ ] 业务事件溯源(Event Sourcing):订单状态变更全留痕
- [ ] CQRS 读写分离:写主库 + 读从库 + 投影到 ES
- [ ] 业务规则引擎:`orders.status` 转换走规则表驱动
- [ ] 多租户隔离:`tenant_id` 字段 + RLS policy

#### F 列
- [ ] 全链路 TraceID 串联:`pg_stat_statements` + OTel
- [ ] 慢查询阈值 < 100ms(从 200ms 收紧)
- [ ] 容量预估脚本:`growth_rate * 3month = capacity_needed`
- [ ] 自动扩容:CPU > 70% 触发,读副本 +1

#### G 列
- [ ] 敏感字段加密:`email / phone / id_card` AES-256-GCM
- [ ] KMS 密钥管理,30 天轮换
- [ ] 数据脱敏:`pg_dump` 导出生产数据自动脱敏
- [ ] 异地多活:RPO < 5s,RTO < 30s

---

## 三、Phase 2 (头部 10万+ DAU) DB 任务 × 9×7 列映射

| Checklist 任务(原行) | 9×7 列 | 二级子模块 | 关键实施点 |
|------------------|--------|----------|----------|
| L142 OceanBase / TiDB | **A 结构** + **G 规则** | A3 分区 + G4 容灾 | OB 多副本 + 自动容灾 |
| L223 MySQL + Redis(画像/特征) | **C 配置** + **F 指标** | C2 索引参数 + F1 QPS | 用户画像 KV + 标签 |

### 🔧 头部阶段 9×7 实战清单(关键 8 项)

| 9×7 列 | 任务 | 验收标准 |
|--------|------|---------|
| A | OB 4.x 替换 PG,水平扩展到 100+ 节点 | 单库 QPS > 100K |
| B | OB 分布式事务 2PC 跨机房 | 99.99% 可用 |
| C | 实时特征计算:Flink + Redis 7 | 用户画像延迟 < 1s |
| D | 压测:百万级并发开播 | 系统不崩溃,P99 < 500ms |
| E | 风控规则实时更新:从 PG 同步到 Redis | 规则生效延迟 < 100ms |
| F | 全链路 OTel + Grafana Tempo 串联 | TraceID 跨服务可查 |
| G | 多活单元化:用户 ID 哈希分 4 单元 | 单元故障不影响整体 |
| C | 向量数据库(推荐/搜索):Milvus / Qdrant | 千人千面实时推荐 |

---

## 四、AI 直播平台 DB 架构演进路线(总览)

```
Phase 0 MVP              Phase 1 规模化            Phase 2 头部
─────────────           ─────────────            ─────────────
PostgreSQL 16            PostgreSQL → 分库分表      OceanBase 4.x
单实例 → 主从             16 库 64 表              100+ 节点
Redis 单实例              Redis Cluster             Redis 7 + 多 AZ
SQLite (本地)            Elasticsearch             Milvus (向量)
-                        Kafka                     Kafka + Flink
-                        OSS + CDN                 多活 + 边缘
```

### 演进铁律(9×7 视角)
1. **A 列**:从单表 → 分区 → 分库分表 → 单元化(每阶段都是 A3 的递进)
2. **B 列**:从单机事务 → 跨库事务 → Saga → 分布式事务(每阶段都是 B2 的递进)
3. **C 列**:从单索引 → 复合/部分 → 异步索引(ES) → 向量索引(每阶段都是 C1 的递进)
4. **F 列**:从指标采集 → 实时监控 → 全链路追踪 → AIOps(每阶段都是 F1-F4 的递进)

---

## 五、Checklist → 9×7 列 索引表(总)

| Checklist 行号 | 任务摘要 | 9×7 列 |
|--------------|---------|--------|
| L82 | KV 存储 SharedPreferences | (客户端,不在 DB 框架内) |
| L83 | 本地数据库 SQLite | (客户端,不在 DB 框架内) |
| L91 | WCDB 加密数据库 | (客户端,不在 DB 框架内) |
| L116 | 数据库 MySQL→PG | A 结构 |
| L117 | 缓存 Redis | F 指标 + C 配置 |
| L118 | 消息队列 Redis Stream | B 逻辑 |
| L119 | 对象存储 OSS | G 规则 |
| L120 | 搜索 MySQL FULLTEXT | C 配置 |
| L127 | 分库分表 ShardingSphere | A 结构 + B 逻辑 |
| L130 | Redis Cluster | F 指标 + G 规则 |
| L131 | 自建 MinIO | G 规则 |
| L132 | 搜索引擎 ES | C 配置 + B 逻辑 |
| L142 | OceanBase / TiDB | A 结构 + G 规则 |
| L223 | 画像存储 MySQL+Redis | C 配置 + F 指标 |
| L257 | 特征平台自研 | C 配置 + D 用例 |
| L300 | 弹幕 WebSocket+Redis | B 逻辑 + F 指标 |
| L302 | 录制 CDN + OSS | G 规则 |
| L311 | 礼物防刷 Redis 排行榜 | B 逻辑 + G 规则 |
| L407 | 分布式事务 Seata/Saga | B 逻辑 + G 规则 |
| L441 | 数据存储 MySQL+OSS | A 结构 + G 规则 |

---

## 六、入库清单

- [x] Phase 0 MVP × 9×7 7 列实战清单
- [x] Phase 1 规模化 × 9×7 7 列实战清单
- [x] Phase 2 头部 × 9×7 7 列实战清单
- [x] DB 架构演进路线图(4 阶段)
- [x] Checklist L# → 9×7 列 总索引表(19 项)
- [ ] Phase 3 商业化 × 9×7 (GMV / 多币种 / 跨境)

---

## 七、关联文档

- [[00-数据库开发全流程-极致深度框架-9×7矩阵]] — 母框架
- [[01-数据库开发全流程-4-7级深度展开-实例]] — 深度实例
- [[00-AI直播平台落地checklist]] — 来源 checklist
- [[00-总索引]] — 项目入口

---

**入库时间**:2026-06-25
**串联方式**:checklist L# → 9×7 列(A-G)双向索引
**价值**:把抽象 checklist 转化为按列组织的可执行任务清单