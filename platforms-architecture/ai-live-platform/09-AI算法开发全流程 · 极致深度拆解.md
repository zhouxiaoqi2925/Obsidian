---
tags:
  item:
    item:
      - AI直播平台
      - AI算法
      - 拆解框架
      - 亚比特级
      - 1111-3
      - 数字人
      - TTS
      - ASR
      - 推荐
source: 1111(3).txt 二、AI算法开发全流程
status: 已完成
applies_to: ai-live-platform/ai-service
title: 09-AI算法开发全流程 · 极致深度拆解
---

# 09 · AI 算法开发全流程 · 极致深度拆解(亚比特级 · 特征级)

> 框架:`G:\Obsidian Vault\Projects\platforms-architecture\00-通用深度拆解框架模板-亚比特级.md`
> 源:`C:\Users\15389\Desktop\1111(3).txt` lines 439-490(细度 10⁻²⁶ 特征级)
> 适用范围:`ai-live-platform/ai-service`(Python FastAPI + PyInstaller)

---

## 〇、9 级 × 7 列骨架

7 列固定映射:
- **A 结构** = 模型结构/网络层
- **B 逻辑** = 前向/反向传播
- **C 配置** = 超参数/优化器
- **D 用例** = 训练数据/标注
- **E 校验** = 评估指标/AB 测试
- **F 指标** = 准确率/QPS/延迟
- **G 规则** = 漂移检测/重训触发

---

## 一级模块(7 大并列)

| # | 一级模块 | 二级数 | AI Live Platform 对应 |
|---|---------|--------|---------------------|
| 1 | 问题需求定义 | 4 | 数字人/推荐/审核三类 |
| 2 | 数据工程处理 | 5 | 主播数据 + 用户行为 |
| 3 | 算法模型设计 | 4 | LLM + TTS + ASR + 数字人 |
| 4 | 训练调优迭代 | 5 | 微调(SFT/LoRA) |
| 5 | 评估验证分析 | 4 | badcase 回收 |
| 6 | 部署推理上线 | 4 | PyInstaller + ONNX |
| 7 | 迭代运维监控 | 5 | 数据漂移 + 重训练 |

---

## 1. 问题需求定义(4 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 1.1 业务问题 | 数字人直播 / 智能客服 / 推荐 / 违规审核 | 已有 | P0 |
| 1.2 指标目标 | 数字人还原度 / 推荐 CTR / 审核准确率 | 待量化 | P0 |
| 1.3 可行性 | 算法复杂度 / 资源成本 | 待评估 | P1 |
| 1.4 排期 | 任务拆分 / 人天 | 1 季度 | P2 |

**核心 AI 能力清单(项目)**
| 能力 | 模型选型 | 训练状态 | 推理方式 |
|------|---------|---------|---------|
| 数字人主播 | MuseTalk / SadTalker + 声音克隆 | 预训练 + 微调 | 实时 GPU |
| 智能客服 | Qwen2.5 / GPT-4 | LoRA 微调 | LLM API |
| TTS 语音 | edge-tts / GPT-SoVITS | 微调 | 实时 |
| ASR 识别 | Whisper / Paraformer | 预训练 | 流式 |
| 商品推荐 | 协同过滤 + 深度学习 | 离线训练 | 实时 |
| 违规审核 | 多模态 CLIP + LLM | 预训练 | 实时 |
| 弹幕过滤 | TextCNN / BERT | 离线 | 实时 |

**待办**
- P0:核心 AI 能力指标量化(数字人 MOS / 推荐 CTR / 审核 F1)
- P1:可行性评估(成本/精度/延迟)

---

## 2. 数据工程处理(5 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 2.1 数据采集标注 | 主播音视频 / 用户行为 / 标注规则 | 缺 | P0 |
| 2.2 数据清洗 | 异常值 / 缺失 / 重复 | 缺 | P0 |
| 2.3 特征工程 | 特征构造 / 降维 / 重要性 | 缺 | P0 |
| 2.4 数据集划分 | train/val/test | 缺 | P0 |
| 2.5 数据增强 | 增广 / 合成 | 缺 | P2 |

**8 比特级数据规格**
- 单标注标签粒度(粗/细)
- 单特征维度数
- 单降维目标维度
- 单标签字符长度
- 单异常值处理方式
- 单特征构造方法
- 单特征重要性评估
- 单特征字节大小

**待办**
- P0:主播数据采集(音视频 + 文案)
- P0:标注规范文档
- P0:数据清洗 pipeline
- P0:特征工程标准化

---

## 3. 算法模型设计(4 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 3.1 算法选型 | 模型家族 / 骨干 / 损失 / 优化器 | LLM/TTS/ASR 各选型 | P0 |
| 3.2 网络结构 | 层数 / 神经元 / 激活 | 待选 | P0 |
| 3.3 损失函数 | 选型 / 权重 | CrossEntropy / L1 / CTC | P0 |
| 3.4 优化策略 | 优化器 / 学习率 | AdamW + Cosine | P0 |

**核心模型选型**

**数字人(主播虚拟形象)**
- 嘴型同步:MuseTalk / SadTalker / Wav2Lip
- 身体动作:Live2D / 3D Diffusion
- 表情驱动:EMOCA / 3DMM

**LLM(智能客服)**
- 开源:Qwen2.5-7B / Llama3-8B / GLM-4-9B
- 闭源:Claude / GPT-4
- 微调:LoRA / QLoRA

**TTS(语音合成)**
- edge-tts(云端,免训练)
- GPT-SoVITS(声音克隆,5s 样本)
- CosyVoice(阿里开源)

**ASR(语音识别)**
- Whisper-large-v3(OpenAI)
- Paraformer(阿里)
- SenseVoice(多语种)

**8 比特级模型参数**
- 单参数初始化值
- 单权重数值精度(FP16 / BF16 / FP32)
- 单偏置项初始值
- 单激活函数斜率
- 单卷积核大小
- 单池化方式选型
- 单残差结构设计
- 单注意力头数

**待办**
- P0:核心模型选型(数字人 / LLM / TTS / ASR)
- P0:训练脚本 + 配置文件

---

## 4. 训练调优迭代(5 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 4.1 环境搭建 | 框架 / GPU / 分布式 | 已有 vllm | P2 |
| 4.2 模型训练 | 训练循环 / 损失 / 评估 | 缺 | P0 |
| 4.3 超参调优 | 学习率 / batch / 轮次 | 缺 | P0 |
| 4.4 正则化 | Dropout / Weight Decay | 缺 | P1 |
| 4.5 训练可视化 | wandb / tensorboard | 缺 | P1 |

**8 比特级训练参数**
- 单学习率配置(1e-4 ~ 5e-5 for LoRA)
- 单批次大小配置(8 / 16 / 32)
- 单训练轮次设置(3 ~ 10 for LoRA)
- 单正则化系数(0.01 ~ 0.1)
- 单梯度更新步长
- 单权重衰减系数(0.01)
- 单动量参数值(0.9)
- 单早停耐心值(3 ~ 5)

**训练配置(LoRA 微调数字人客服)**
```yaml
model: Qwen2.5-7B
lora_rank: 16
lora_alpha: 32
lora_dropout: 0.05
learning_rate: 2e-4
batch_size: 8
gradient_accumulation: 4
epochs: 3
warmup_ratio: 0.03
weight_decay: 0.01
max_seq_length: 4096
precision: bf16
```

**待办**
- P0:LLM LoRA 微调 pipeline
- P0:TTS 声音克隆训练
- P1:wandb 接入可视化

---

## 5. 评估验证分析(4 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 5.1 指标体系 | 准确率 / F1 / NDCG / MOS | 待定 | P0 |
| 5.2 离线评估 | 验证集 / 测试集 | 缺 | P0 |
| 5.3 在线 AB | 流量分桶 / 显著性 | 缺 | P0 |
| 5.4 badcase | 错误分析 / 改进 | 缺 | P0 |

**8 比特级评估指标**
- 单指标计算公式
- 单指标阈值设定
- 单指标小数精度
- 单指标置信度
- 单指标对比分析
- 单实验流量占比
- 单 AB 实验配置
- 单显著性检验

**核心 AI 能力 SLO**
| 能力 | 指标 | 目标 |
|------|------|------|
| 数字人 | 嘴型同步 SSIM | ≥ 0.85 |
| 数字人 | 声音 MOS | ≥ 4.0 |
| LLM 客服 | 意图识别 F1 | ≥ 0.90 |
| TTS | 自然度 MOS | ≥ 4.0 |
| ASR | 中文 WER | ≤ 8% |
| 推荐 | CTR 提升 | ≥ 15% |
| 审核 | 准确率 | ≥ 95% |
| 弹幕过滤 | 召回率 | ≥ 90% |

**待办**
- P0:核心 AI 能力评估 SOP
- P0:badcase 回收 + 分析 pipeline

---

## 6. 部署推理上线(4 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 6.1 模型导出 | ONNX / TensorRT / TorchScript | 已有 PyInstaller | P0 |
| 6.2 推理优化 | 量化 / 剪枝 / 蒸馏 | 待补 | P0 |
| 6.3 服务部署 | 容器化 / GPU 调度 | FastAPI + K8s | P0 |
| 6.4 接口封装 | REST / gRPC / WebSocket | 已有 FastAPI | P0 |

**8 比特级推理参数**
- 单量化位宽配置(INT8 / INT4)
- 单剪枝比例配置(50% / 70%)
- 单推理引擎参数(vllm / tgi / sglang)
- 单接口响应耗时(< 500ms)
- 单并发推理数
- 单量化比特数(4 / 8)
- 单剪枝阈值精度
- 单推理加速比

**部署架构(项目)**
```
ai-service/
├── app/
│   ├── main.py            # FastAPI 入口
│   ├── models/            # 模型加载
│   ├── inference/         # 推理服务
│   ├── tts/               # TTS 模块
│   ├── asr/               # ASR 模块
│   ├── digital_human/     # 数字人
│   └── recommend/         # 推荐
├── models/                # 预训练权重
├── scripts/build.sh       # PyInstaller 打包
└── pyproject.toml
```

**模型部署矩阵**
| 模型 | 量化 | 推理引擎 | 延迟 | 显存 |
|------|------|---------|------|------|
| LLM 7B | INT4 | vllm | 50ms | 8GB |
| TTS | - | edge-tts | 200ms | 0 |
| ASR | FP16 | faster-whisper | 300ms | 2GB |
| 数字人 | FP16 | Triton | 100ms | 4GB |

**待办**
- P0:vllm 部署 LLM(INT4 量化)
- P0:Triton 部署数字人(FP16)
- P1:模型量化(INT4 / INT8)
- P1:模型剪枝

---

## 7. 迭代运维监控(5 二级)

| 二级 | 三级 | 项目对应 | 优先级 |
|------|------|---------|-------|
| 7.1 效果监控 | 线上指标 / 异常检测 | Prometheus | P0 |
| 7.2 数据漂移 | KS 检验 / PSI / 分布 | 缺 | P0 |
| 7.3 模型迭代 | 重训练 / A/B / 灰度 | 缺 | P0 |
| 7.4 算力调度 | GPU 池化 / 自动扩缩 | 缺 | P1 |
| 7.5 资产沉淀 | Model Zoo / 训练集管理 | 缺 | P2 |

**8 比特级监控**
- 单指标波动监控(±5%)
- 单漂移检测阈值(PSI > 0.2)
- 单漂移阈值小数位
- 单重训练触发条件(连续 3 天 PSI > 0.2)
- 单指标精度位
- 单特征级漂移检测
- 单 case 字段粒度
- 单周期天级精度

**漂移检测 SOP**
- PSI(Population Stability Index)阈值 > 0.2
- 连续 3 天 PSI > 0.2 触发告警
- 每周对比 feature distribution
- 月度模型重训评估

**待办**
- P0:线上效果监控(7 个核心指标)
- P0:数据漂移检测 + 重训练触发
- P1:GPU 自动扩缩容
- P2:Model Zoo

---

## 总结:AI 服务架构

```
用户 → WebSocket → ai-service (FastAPI)
                       ↓
        ┌──────────────┼──────────────┐
        ↓              ↓              ↓
     LLM(TTS)    数字人(MuseTalk)  推荐(协同过滤)
        ↓              ↓              ↓
       vllm          Triton          PG
```

**9 亚比特级 AI 训练清单**
- 单字符类别语义
- 单数值精度位(FP32 / FP16 / INT8)
- 单算法时间复杂度(O(n²) / O(n log n))
- 单显存占用估算(7B INT4 = 5GB)
- 单特征字节大小(1024 dim × 4 byte = 4KB)
- 单维度整数精度(int32)
- 单激活函数斜率(ReLU / GELU)
- 单比特相位差
- 单毫秒级指标监控

---

## P0/P1/P2 Action Items

**P0(立即)**
1. 7 个核心 AI 能力指标量化
2. 主播数据采集 + 标注规范
3. 核心模型选型(LLM/TTS/ASR/数字人)
4. 训练 pipeline + LoRA 微调
5. vllm + Triton 部署
6. 效果监控 + 漂移检测
7. badcase 回收 pipeline

**P1(2 周)**
8. 数据增强(音视频增广)
9. 模型量化(INT4/INT8)
10. 在线 AB 测试框架
11. GPU 自动扩缩容

**P2(1 季度)**
12. 模型剪枝 + 蒸馏
13. 多模态(CLIP 审核)
14. Model Zoo
15. AutoML(自动超参)

---

## 与项目代码对应

```
ai-service/
├── app/
│   ├── main.py                  OK
│   ├── models/                  P0:核心模型加载
│   ├── inference/               P0:推理服务
│   ├── tts/                     P0:edge-tts 集成
│   ├── asr/                     P0:whisper 集成
│   ├── digital_human/           P0:数字人模块
│   └── recommend/               P0:推荐算法
├── models/                      P0:预训练权重
├── data/                        P0:训练数据
├── scripts/
│   ├── train_lora.py            P0:待新建
│   ├── build.sh                 OK:PyInstaller
│   └── eval.py                  P0:待新建
├── requirements.txt             P0:删 7 个死依赖
└── pyproject.toml               OK
```

